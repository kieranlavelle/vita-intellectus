version: "3"
services:
    vita-fe:
        container_name: vita-fe
        build:
            context: .
            dockerfile: Dockerfile
        ports:
            - 80:80
        networks:
            - reverse-proxy-network

networks:
    reverse-proxy-network:
        external: true